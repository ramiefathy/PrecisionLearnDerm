/**
 * Stub hooks for AI test endpoints.
 * These are only used locally in development; disabled in production.
 */
export function test_generate_question() {
  throw new Error('Test endpoints disabled in production');
}

export function test_review_question() {
  throw new Error('Test endpoints disabled in production');
}